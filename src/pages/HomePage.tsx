import SearchBar from '../components/SearchBar';

const HomePage = () => {
    return (
        <main>
            <h1>지역 날씨 검색</h1>
            <SearchBar />
        </main>
    );
};

export default HomePage;
